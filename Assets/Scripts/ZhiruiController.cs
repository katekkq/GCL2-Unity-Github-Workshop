using UnityEngine;

public class ZhiruiController : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        print("i am a cute alien");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
