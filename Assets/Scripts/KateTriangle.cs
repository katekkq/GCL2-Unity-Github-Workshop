using UnityEngine;

public class KateTriangle : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log("I'm a triangle");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
